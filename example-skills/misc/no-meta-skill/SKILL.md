# Skill Without Metadata

This skill intentionally ships without a `skill.yml` so we can demonstrate that
the catalogue still lists it — with `unknown` placeholders for the missing
fields.

The directory itself is also missing version/author info; only the body is here.
